# Not used at runtime — the app bypasses Spring Boot entirely and runs as a
# plain Java console program (see OopFundamentalsApplication.main). Left here
# just so Spring tooling doesn't complain if you ever wire Spring back in.
spring.application.name=OOPFundamentals
spring.main.banner-mode=off
